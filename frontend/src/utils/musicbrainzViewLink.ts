export function buildMusicbrainzReleaseViewLink(mbid: string): string {
  return `https://musicbrainz.org/release/${mbid}`
}

export function buildMusicbrainzArtistViewLink(mbid: string): string {
  return `https://musicbrainz.org/artist/${mbid}`
}
